info:
  name: expose iperf
  type: http
  seq: 2

http:
  method: POST
  url: http://192.168.122.223:8080/api/v1/expose
  body:
    type: json
    data: |-
      {
        "container_ip": "172.20.0.20",
        "container_port": 5201
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
